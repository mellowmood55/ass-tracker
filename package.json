{
  "name": "ass-tracker",
  "private": true,
  "version": "1.0.0",
  "description": "ICT Asset Tracker — cloud-ready inventory for ICT teams",
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:lan": "concurrently -n server,client -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client -- --host\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
